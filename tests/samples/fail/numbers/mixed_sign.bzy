// Mixed-signedness arithmetic itself is allowed (a + b promotes to uint, C-style
// "unsigned wins"); the rejection here is assigning that uint result back to an
// int without a cast.
void main()
{
	int a;
	uint b;
	int c;
	c = a + b;
}
