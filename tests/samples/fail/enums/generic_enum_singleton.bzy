// A generic enum's constants must carry a payload for now; a zero-field variant
// (like Option's None) needs construction-site type inference.
enum Opt<T>
{
	Some(T v),
	None;
}

void main()
{
	print(1);
}
