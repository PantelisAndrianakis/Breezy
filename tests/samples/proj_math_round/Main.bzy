void main()
{
	print(Math.floor(2.7));   // 2
	print(Math.ceil(2.1));    // 3
	print(Math.round(2.4));   // 2
	print(Math.round(2.6));   // 3
	print(Math.toRadians(180.0) > 3.1);   // true  (PI > 3.1)
	print(Math.toRadians(180.0) > 3.2);   // false (PI < 3.2)
}
