// Read an environment variable. PATH is always present on both OSes, so
// this exercises the value path without needing null-string handling.
void main()
{
	string p = System.getenv("PATH");
	if (p.length() > 0)
	{
		print(1);
	}
	else
	{
		print(0);
	}
}
